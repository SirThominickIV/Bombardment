extends Node

# GDScript doesn't permit const enums, so this format is as good as it gets

const DestroyedTiles = "DestroyedTiles"
const Ground = "Ground"
const IrradiatedGround = "IrradiatedGround"
const Foreground = "Foreground"
const Selection = "Selection"
